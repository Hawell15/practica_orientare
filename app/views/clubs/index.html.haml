%h1{ style: "text-align:center; color:green"} Clubs List

%table{class: "table table-striped table-bordered table-hover"}
  %thead
    %tr{class: "table-primary"}
      %th Name
      %th Territory
      %th Representative
      %th Email
      %th Phone
      %th{ colspan: "3"}Actions
  %tbody
    - @clubs.each do |club|
      %tr
        %td= club.club_name
        %td= club.territory
        %td= club.representative
        %td= club.email
        %td= club.phone
        = render partial: '/layouts/actions', :locals => { :element => club }
= link_to 'New Club', new_club_path, class: "btn btn-info"
