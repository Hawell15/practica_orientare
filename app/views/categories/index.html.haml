%h1{ style: "text-align:center; color:green"} Categories List

%table{class: "table table-striped table-bordered table-hover"}
  %thead
    %tr{class: "table-primary"}
      %th Name
      %th Full name
      %th Points
      %th{ colspan: "3"}Actions
  %tbody
    - @categories.each do |category|
      %tr
        %td= category.name
        %td= category.full_name
        %td= category.points
        = render partial: '/layouts/actions', :locals => { :element => category }
= link_to 'New Category', new_category_path, class: "btn btn-info"

