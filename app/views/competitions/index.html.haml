%h1{ style: "text-align:center; color:green"} Competition List

%table{class: "table table-striped table-bordered table-hover"}
  %thead
    %tr{class: "table-primary"}
      %th Name
      %th Date
      %th Location
      %th Country
      %th Distance type
      %th{ colspan: "3"}Actions
  %tbody
    - @competitions.each do |competition|
      %tr
        %td= competition.competition_name
        %td= competition.date
        %td= competition.location
        %td= competition.country
        %td= competition.distance_type
        = render partial: '/layouts/actions', :locals => { :element => competition }
- if user_signed_in?
  = link_to 'New Competition', new_competition_path, class: "btn btn-info"

