%h1{ style: "text-align:center; color:green"} Groups List

%table{class: "table table-striped table-bordered table-hover"}
  %thead
    %tr{class: "table-primary"}
      %th Name
      %th Clasa
      %th Rang
      %th Competition
      %th{ colspan: "3"}Actions
  %tbody
    - @groups.each do |group|
      %tr
        %td= group.group_name
        %td= group.clasa
        %td= group.rang
        %td= link_to group.competition.competition_name, competition_path(group.competition)
        = render partial: '/layouts/actions', :locals => { :element => group }
= link_to 'New Group', new_group_path, class: "btn btn-info"
